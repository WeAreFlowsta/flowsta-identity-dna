{
  "name": "flowsta-identity-dna",
  "scripts": {
    "build:zomes": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown",
    "build:dna": "hc dna pack workdir",
    "build:happ": "hc app pack workdir",
    "build": "npm run build:zomes && npm run build:dna && npm run build:happ"
  }
}
